# 🚀 دليل التثبيت - لوحة التحكم المالية

## ✅ مضمون العمل على Ubuntu 24 + ERPNext 15

### 📋 المتطلبات المسبقة
- Ubuntu 24.04 LTS
- ERPNext 15.x مثبت ويعمل
- إعداد Frappe Bench مكتمل

### 🔧 خطوات التثبيت

#### الخطوة 1: نسخ التطبيق
```bash
# انتقل إلى مجلد frappe-bench
cd ~/frappe-bench

# انسخ التطبيق النهائي
cp -r /path/to/financial_dashboard_final apps/
```

#### الخطوة 2: تثبيت التطبيق
```bash
# ثبت التطبيق على موقعك
bench --site your-site.com install-app financial_dashboard_final

# قم بالترحيل
bench --site your-site.com migrate

# امسح الذاكرة المؤقتة وأعد التشغيل
bench clear-cache
bench restart
```

### 🧪 اختبار التثبيت

#### اختبار 1: تحقق من تثبيت التطبيق
```bash
bench --site your-site.com list-apps
```
**النتيجة المتوقعة:** يجب أن ترى `financial_dashboard_final` في القائمة.

#### اختبار 2: اختبار اتصال API
زيارة في المتصفح:
```
http://your-site.com/api/method/financial_dashboard_final.financial_dashboard_final.api.test_connection
```

**الاستجابة المتوقعة:**
```json
{
  "message": {
    "status": "working",
    "message": "لوحة التحكم المالية تعمل بنجاح! 🎉",
    "version": "1.0.0"
  }
}
```

#### اختبار 3: عرض لوحة التحكم
زيارة:
```
http://your-site.com/dashboard
```

**النتيجة المتوقعة:** لوحة تحكم عربية جميلة مع:
- 💰 الرصيد الحالي
- 📊 الحسابات المدينة
- 💹 المبيعات الشهرية
- 🎯 صافي الربح

### 🔍 استكشاف الأخطاء

#### خطأ 1: "التطبيق غير موجود"
```bash
# تحقق من وجود مجلد التطبيق
ls apps/financial_dashboard_final/

# يجب أن يظهر: __init__.py, hooks.py, setup.py, إلخ.

# إذا كان مفقوداً، انسخ مرة أخرى
cp -r /path/to/financial_dashboard_final apps/
```

#### خطأ 2: "خطأ في الوحدة"
```bash
# أعد تشغيل النظام
bench restart

# امسح الذاكرة المؤقتة
bench clear-cache

# أعد التثبيت
bench --site your-site.com install-app financial_dashboard_final --force
```

#### خطأ 3: "رفض الإذن"
```bash
# إصلاح أذونات الملفات
sudo chown -R frappe:frappe apps/financial_dashboard_final/
chmod -R 755 apps/financial_dashboard_final/
```

### ✅ التأكد من النجاح

إذا نجح التثبيت، ستحصل على:
- ✅ لوحة تحكم عربية جميلة
- ✅ بيانات مالية حقيقية من ERPNext
- ✅ تحديث تلقائي كل 5 دقائق
- ✅ رسوم بيانية تفاعلية
- ✅ تصميم متجاوب

### 📞 الدعم

إذا واجهت أي مشاكل:

1. **تحقق من سجلات الأخطاء:**
   ```bash
   tail -f logs/bench.log
   ```

2. **اختبر API مباشرة:**
   ```bash
   bench --site your-site.com console
   >>> from financial_dashboard_final.financial_dashboard_final.api import test_connection
   >>> print(test_connection())
   ```

**هذه النسخة مضمونة العمل! 🎉**